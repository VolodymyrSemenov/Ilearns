void begin_game_find_numbers(int number_of_){
    // Start the game where the player has to find the numbers
    // Turn off button LEDs of other games.

    // Loop through each reader. Check if they are the correct letter.
    // Keep a count: if all are correct, start win code 
}