void begin_game_number_ordering(){
    // Start the game where the player has to order the numbers
    // Turn off button LEDs of other games.

    // Loop through each reader. Check if they are the correct number.
    // Keep a count: if all are correct, start win code 
}